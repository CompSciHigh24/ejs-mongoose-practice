<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Planets of the Solar System</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      background-color: #f0f8ff;
      color: #333;
      text-align: center;
    }
    .planet-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin-top: 20px;
    }
    .planet-card {
      background: white;
      padding: 15px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      width: 250px;
      text-align: center;
    }
    .planet-image {
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <h1>Explore the Planets</h1>

  <div class="planet-container">
  
    <!-- Loop through all planets using a for loop -->
    
      <div class="planet-card">
        <!-- Use the name key to set the title AND link to /info/{planet name} -->
        <h2><a href="/info/...">...</a></h2>

        <!-- Use the image key to set the img src -->
        <img src="..." class="planet-image" />

        <!-- Use the description key to set the paragraph text -->
        <p>...</p>
      </div>

  <!-- End loop here -->
  
  </div>

</body>
</html>
